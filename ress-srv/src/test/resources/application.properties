server.port=0
server.address=0.0.0.0

# Actuator
endpoints.shutdown.enabled=true
endpoints.health.sensitive=false

# Spring config
spring.config.additional-location=classpath:cfg-cas.properties,classpath:cassandra_connection.properties
